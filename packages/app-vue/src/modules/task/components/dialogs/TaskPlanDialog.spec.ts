import { describe, expect, it } from 'vitest'; import fs from 'node:fs'; import path from 'node:path'; describe('TaskPlanDialog vNext',()=>{it('uses flat Task plan form without DAG controls',()=>{const source=fs.readFileSync(path.resolve(__dirname,'TaskPlanDialog.vue'),'utf8');expect(source).toContain('TaskPlanForm');for(const retired of ['DependencyManager','parentTaskId','folderId','TaskForDAG']) expect(source).not.toContain(retired);});});
