{
  // 继承基础配置
  "extends": "../../tsconfig.workspace-src.json",

  "compilerOptions": {
    // ============================================================
    // 路径和输出配置
    // ============================================================

    // 输出目录：编译后的文件


    // 源码根目录：限制编译范围


    // ============================================================
    // Vite 构建模式配置
    // ============================================================

    // 不使用 tsc 输出，由 vite 统一处理
    "noEmit": true,

    // 禁用增量编译（noEmit 时不能使用 incremental）
    "incremental": false,

    // ============================================================
    // 库环境配置（Assets 静态资源包）
    // ============================================================

    // 标准库：包含 ES2020 和 DOM（用于 URL 等浏览器 API）
    "lib": ["ES2020", "DOM"],

    // 类型定义：包含 Vite 客户端类型
    // 用于处理静态资源导入（如图片、音频等）
    "types": ["vite/client"],

    // 模块解析：使用 Bundler 策略
    // 因为资源文件由 Vite 打包工具处理
    "moduleResolution": "Bundler",

    // ============================================================
    // 本地路径别名配置
    // ============================================================

    // 路径别名：仅用于包内部导入
    "paths": {
      "@/*": ["./src/*"]
    }
  },

  // ============================================================
  // 包含和排除配置
  // ============================================================

  // 包含的文件：所有 src 目录下的文件
  "include": ["src/**/*"],

  // 排除的文件
  "exclude": ["node_modules", "dist"]
}
