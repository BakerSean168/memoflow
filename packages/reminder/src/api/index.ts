/**
 * Routine vNext HTTP transport seam.
 *
 * R4-2201C intentionally exposes no legacy Reminder HTTP routes. Routine
 * commands are currently composed through host-owned application ports.
 */
export {};
