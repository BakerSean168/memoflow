/**
 * Routine vNext HTTP transport marker.
 *
 * R4-2201C intentionally exposes no legacy Reminder HTTP routes. The file is
 * retained only as the server-first feature package audit target; canonical
 * Routine commands are composed through host-owned application ports.
 */
export {};
