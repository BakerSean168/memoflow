/** Notification Fact client contract. Delivery execution is queried separately. */
import type { NotificationServerDTO } from './notification-server';
export type NotificationClientDTO = NotificationServerDTO;
